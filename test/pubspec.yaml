name: better_proto_generator_test
description: A starting point for Dart libraries or applications.
environment:
  sdk: ">=3.2.4 <4.0.0"
publish_to: none

dependencies:
  decimal: ^2.3.0

  better_proto_generator:
    path: ../better_proto_generator
  better_proto_annotations:
    path: ../better_proto_annotations
  grpc: ^3.0.2
  yaml: ^3.1.1
  protobuf: ^3.0.0
  squarealfa_generators_common: ^3.1.1

dev_dependencies:
  build_runner: ^2.4.7
  build_verify: ^3.1.0
  lints: ^3.0.0
  test: ^1.25.0
